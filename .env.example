# Environment variables for the Sesame Voice Agent

# Required for downloading models like Llama 3.2 and potentially CSM 1B
# Get your token from Hugging Face Settings -> Access Tokens
HUGGING_FACE_TOKEN="YOUR_HUGGING_FACE_READ_TOKEN_HERE"

# Optional: Override config paths (rarely needed if using default structure)
# CONFIG_PATH="config/custom_config.yaml" 